# 📝 Управление контентом

## Структура папок

```
content/
└── quizzes/           # JSON файлы с квизами
    ├── _template.json # Шаблон для нового квиза
    ├── quiz-1-*.json  # Квиз 1
    ├── quiz-2-*.json  # Квиз 2
    └── ...
```

## Быстрый старт

### 1. Создать новый квиз

1. Скопируйте `_template.json` в новый файл:
   ```bash
   cp content/quizzes/_template.json content/quizzes/quiz-6-my-quiz.json
   ```

2. Отредактируйте файл, заполнив:
   - `title` - название квиза
   - `description` - описание
   - `prizeTitle` - название приза
   - `questions` - массив вопросов

### 2. Загрузить в базу данных

```bash
# Импорт всех новых квизов
npm run content:import

# Импорт с очисткой старых данных
npm run content:import:clear

# Посмотреть список квизов в БД
npm run content:list
```

## Формат квиза

```json
{
  "title": "Название квиза",
  "description": "Описание (1-2 предложения)",
  "prizeTitle": "Название приза",
  "prizeDescription": "Подробное описание приза",
  "isActive": true,
  "questions": [
    {
      "text": "Текст вопроса?",
      "difficulty": 2,
      "timeLimitSeconds": 30,
      "answers": [
        { "text": "Неправильный ответ", "isCorrect": false },
        { "text": "Правильный ответ", "isCorrect": true },
        { "text": "Неправильный ответ", "isCorrect": false },
        { "text": "Неправильный ответ", "isCorrect": false }
      ]
    }
  ]
}
```

## Поля

### Квиз

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `title` | string | ✅ | Название квиза |
| `description` | string | ❌ | Описание |
| `prizeTitle` | string | ✅ | Название приза |
| `prizeDescription` | string | ❌ | Описание приза |
| `isActive` | boolean | ❌ | Активен ли (по умолчанию `true`) |
| `startsAt` | string | ❌ | Дата начала (ISO формат) |
| `endsAt` | string | ❌ | Дата окончания (ISO формат) |
| `questions` | array | ✅ | Массив вопросов |

### Вопрос

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `text` | string | ✅ | Текст вопроса |
| `difficulty` | number | ❌ | Сложность: 1-лёгкий, 2-средний, 3-сложный |
| `timeLimitSeconds` | number | ❌ | Время на ответ (по умолчанию 30) |
| `answers` | array | ✅ | Массив из 4 вариантов ответа |

### Ответ

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `text` | string | ✅ | Текст ответа |
| `isCorrect` | boolean | ✅ | Правильный ли ответ |

⚠️ **Важно:** В каждом вопросе должен быть ровно ОДИН правильный ответ!

## Примеры

### Лёгкий вопрос (difficulty: 1)
```json
{
  "text": "Какого цвета небо?",
  "difficulty": 1,
  "timeLimitSeconds": 20,
  "answers": [
    { "text": "Красное", "isCorrect": false },
    { "text": "Голубое", "isCorrect": true },
    { "text": "Зелёное", "isCorrect": false },
    { "text": "Жёлтое", "isCorrect": false }
  ]
}
```

### Сложный вопрос (difficulty: 3)
```json
{
  "text": "В каком году был принят первый закон о дактилоскопии в России?",
  "difficulty": 3,
  "timeLimitSeconds": 45,
  "answers": [
    { "text": "1902", "isCorrect": false },
    { "text": "1906", "isCorrect": true },
    { "text": "1914", "isCorrect": false },
    { "text": "1920", "isCorrect": false }
  ]
}
```

## Советы по контенту

1. **Баланс сложности** - в каждом квизе должны быть вопросы разной сложности
2. **4 варианта** - всегда давайте 4 варианта ответа
3. **Чёткие формулировки** - вопрос должен быть понятным
4. **Проверяйте факты** - убедитесь в правильности ответов
5. **Интересные призы** - мотивируйте пользователей играть

## Валидация

Скрипт импорта автоматически проверяет:
- ✅ Наличие обязательных полей
- ✅ Ровно один правильный ответ в каждом вопросе
- ✅ Корректность JSON формата

При ошибках импорт остановится с описанием проблемы.

